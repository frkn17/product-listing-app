import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="w-full flex justify-center mb-10">
      <h1 className="productListHeader text-black">Product List</h1>
    </header>
  );
};

export default Header;
